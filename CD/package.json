{
  "name": "cd",
  "private": true,
  "scripts": {
    "dev": "npm --prefix .. run dev",
    "build": "npm --prefix .. run build",
    "preview": "npm --prefix .. run preview",
    "lint": "npm --prefix .. run lint",
    "install:app": "npm install --prefix .."
  }
}
